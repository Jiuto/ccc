/**index.wxss**/
page {
  height: 100vh;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  display: flex;
  justify-content: center;
  align-items: center;
}

.page-container {
  width: 100%;
  min-height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 40rpx;
  box-sizing: border-box;
}

.card-swiper {
  width: 100%;
  height: 100vh;
}

.card-swiper swiper-item {
  height: 100%;
  box-sizing: border-box;
}

.card-scroll {
  width: 100%;
  height: 100%;
  box-sizing: border-box;
}

.card-scroll food-card,
.card-scroll activity-card,
.card-scroll drunk-card {
  display: flex;
  justify-content: center;
  min-height: 100%;
  align-items: center;
  padding: 40rpx 0;
  box-sizing: border-box;
}

/* 庆祝特效 */
.celebrate-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.85);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  z-index: 999;
  animation: fadeIn 0.3s ease-out;
}

.celebrate-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  animation: bounceIn 0.6s cubic-bezier(0.68, -0.55, 0.265, 1.55);
}

.celebrate-emoji {
  font-size: 120rpx;
  animation: bounce 1s infinite;
}

.celebrate-emoji2 {
  font-size: 80rpx;
  margin-top: 20rpx;
  animation: swing 1s infinite;
}

.celebrate-result {
  font-size: 72rpx;
  font-weight: bold;
  color: #feca57;
  margin-top: 20rpx;
  text-shadow: 0 0 30rpx rgba(254, 202, 87, 0.8);
  animation: pulse 1.5s infinite;
}

/* 悲伤风格结果 */
.sad-result {
  color: #5f6c7b;
  text-shadow: 0 0 30rpx rgba(95, 108, 123, 0.6);
  animation: sadPulse 1.5s infinite;
}

.sparkles {
  position: absolute;
  width: 100%;
  height: 100%;
  pointer-events: none;
}

.sparkle {
  position: absolute;
  font-size: 60rpx;
  animation: sparkle 2s infinite;
}

.s1 { top: 15%; left: 10%; animation-delay: 0s; }
.s2 { top: 20%; right: 15%; animation-delay: 0.3s; }
.s3 { top: 60%; left: 8%; animation-delay: 0.6s; }
.s4 { top: 70%; right: 10%; animation-delay: 0.9s; }
.s5 { top: 40%; left: 50%; animation-delay: 1.2s; }

/* 悲伤特效 */
.sad-effects {
  position: absolute;
  width: 100%;
  height: 100%;
  pointer-events: none;
}

.raindrop {
  position: absolute;
  font-size: 50rpx;
  animation: rainDrop 1.5s infinite;
}

.r1 { top: 10%; left: 15%; animation-delay: 0s; }
.r2 { top: 15%; right: 20%; animation-delay: 0.3s; }
.r3 { top: 50%; left: 10%; animation-delay: 0.6s; }
.r4 { top: 55%; right: 15%; animation-delay: 0.9s; }
.r5 { top: 75%; left: 50%; animation-delay: 1.2s; }

.btn {
  background: linear-gradient(135deg, #606af0ff 0%, #e389f7ff 100%);
  color: white;
  border: none;
  padding: 24rpx 60rpx;
  font-size: 36rpx;
  border-radius: 60rpx;
  margin-top: 10rpx;
}

.btn::after {
  border: none;
}

.close-celebrate-btn {
  margin-top: 80rpx;
  background: linear-gradient(135deg, rgb(247, 203, 8) 0%, rgb(247, 152, 89) 100%);
  animation: fadeInUp 0.5s ease-out 0.3s both;
}

/* 悲伤风格按钮 */
.sad-btn {
  background: linear-gradient(135deg, #5f6c7b 0%, #7a8b99 100%);
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes bounceIn {
  0% { transform: scale(0); opacity: 0; }
  50% { transform: scale(1.2); }
  100% { transform: scale(1); opacity: 1; }
}

@keyframes bounce {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-20rpx); }
}

@keyframes swing {
  0%, 100% { transform: rotate(-10deg); }
  50% { transform: rotate(10deg); }
}

@keyframes pulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.05); }
}

@keyframes sparkle {
  0%, 100% { opacity: 0; transform: scale(0.5) rotate(0deg); }
  50% { opacity: 1; transform: scale(1.2) rotate(180deg); }
}

@keyframes fadeInUp {
  from { opacity: 0; transform: translateY(40rpx); }
  to { opacity: 1; transform: translateY(0); }
}

@keyframes sadPulse {
  0%, 100% { transform: scale(1); opacity: 0.7; }
  50% { transform: scale(0.95); opacity: 1; }
}

@keyframes rainDrop {
  0% { opacity: 0; transform: translateY(-20rpx); }
  50% { opacity: 1; }
  100% { opacity: 0; transform: translateY(20rpx); }
}

@keyframes cloudFloat {
  0%, 100% { transform: translateX(-50%) rotate(0deg); }
  50% { transform: translateX(-50%) rotate(5deg); }
}
